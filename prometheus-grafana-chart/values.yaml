kube-prometheus-stack:
  grafana:
    adminPassword: admin
    service:
      type: ClusterIP
    ingress:
      enabled: true
      hosts:
        - grafana.local.tcloud
  prometheus:
    service:
      type: ClusterIP
  alertmanager:
    enabled: false
